class NewCarModel {
  int id;
  String title;
  String description;
  String startedPrice;
  String images;
  String link;

  NewCarModel(this.id, this.title, this.description, this.startedPrice,
      this.images, this.link);
}
